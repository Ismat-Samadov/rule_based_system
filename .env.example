# =============================================================================
# AgriAdvisor Agricultural Advisory System - Environment Variables
# =============================================================================
# Copy this file to .env and update with your actual values
#
# IMPORTANT: Never commit the .env file to version control!
# The .env file contains sensitive information like API keys.
# =============================================================================

# -----------------------------------------------------------------------------
# BACKEND ENVIRONMENT VARIABLES (Used by FastAPI backend in /backend)
# -----------------------------------------------------------------------------

# Google Gemini API Key (REQUIRED for AI chatbot)
# Get your free API key from: https://aistudio.google.com/app/apikey
# The chatbot will NOT work without this key
GEMINI_API_KEY=your_gemini_api_key_here

# Debug Mode (Optional)
# Set to True for development, False for production
# Default: True
DEBUG=True

# CORS Origins (Optional)
# Comma-separated list of allowed frontend URLs
# Example: https://yonca-frontend.vercel.app,https://yonca.az
# Default: http://localhost:3000
CORS_ORIGINS=http://localhost:3000

# -----------------------------------------------------------------------------
# FRONTEND ENVIRONMENT VARIABLES (Used by Next.js frontend in /frontend)
# -----------------------------------------------------------------------------
# Note: Frontend variables must be prefixed with NEXT_PUBLIC_

# Backend API URL (REQUIRED)
# Local development: http://localhost:8000
# Production: https://your-backend.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Get Gemini API Key:
#    - Visit: https://aistudio.google.com/app/apikey
#    - Create a new API key (free tier available)
#    - Copy the key and replace 'your_gemini_api_key_here'
#
# 3. Update URLs if needed:
#    - For local development, the defaults should work
#    - For production, update CORS_ORIGINS and NEXT_PUBLIC_API_URL
#
# 4. Verify setup:
#    - Backend: python3 -m uvicorn app.main:app --reload
#    - Frontend: npm run dev
#    - Visit: http://localhost:3000
#
# =============================================================================
